export interface User {
  id: string;
  name: string;
  email: string;
  type: number;
}

export interface ContractorPost {
  id: string;
  title: string;
  phone: string;
  description: string;
  labourCount: number;
}
